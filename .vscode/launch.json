{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/push_swap_debug",
            "args": ["2", "4", "1", "9"],
            "cwd": "${workspaceFolder}"
        }
    ]
}